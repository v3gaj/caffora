<% if @collection.errors.any? %>
  	$(".collection_errors").html("<%= j (render 'errors') %>");
<% else %>
	$(".collection_table").html("<%= j (render 'table') %>");
	$(".notice").slideToggle().html("Collection updated succesfully.").delay(2000).slideToggle();
<% end %>