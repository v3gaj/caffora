<% if @post.errors.any? %>
  	$('.post_errors').html("<%= j (render 'errors') %>");
<% else %>
	$(".post_table").html("<%= j (render 'table') %>");
	$(".post_form").slideToggle();
	$(".notice").slideToggle().html("Post created succesfully.").delay(2000).slideToggle();
<% end %>